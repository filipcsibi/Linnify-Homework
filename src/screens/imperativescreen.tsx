import {Image, Pressable, View} from 'react-native';
import {TextCompDetailsRef} from '../components/MovieDetails';

export const MyImperativeScreen = () => {
  return (
    <View>
      <View
        style={{
          maxHeight: '55%',
          maxWidth: '100%',
        }}>
        <Pressable></Pressable>
      </View>
    </View>
  );
};
